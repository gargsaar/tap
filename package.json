{
  "name": "tap-meeting-recorder",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Record Google Meet meetings — mic + tab audio — in the browser",
  "dependencies": {
    "@vercel/blob": "^1.1.1"
  }
}
